---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data-test-db-node-0
  namespace: test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      system: test-db
      node: node-0

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data-test-db-node-1
  namespace: test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      system: test-db
      node: node-1

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data-test-db-node-2
  namespace: test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      system: test-db
      node: node-2

